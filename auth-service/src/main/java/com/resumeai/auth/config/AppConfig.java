package com.resumeai.auth.config;

import org.springframework.context.annotation.Configuration;

/**
 * Application-level beans.
 * ModelMapper was removed — UserAuthService no longer depends on it.
 */
@Configuration
public class AppConfig {
    // intentionally empty — add beans here if needed
}
